---
title: "Configuration"
description: "Environment variables and global flags."
weight: 20
---

devdocs aims to need almost no configuration. As you add settings,
document each one here with its default.

## Global flags

| Flag | Meaning |
|---|---|
| `--help` | Help for any command |
| `--version` | Print the version |

## Environment variables

None yet. Add them here as the tool grows, prefixed `devdocs_` in upper
case (for example `devdocs_TOKEN`).
